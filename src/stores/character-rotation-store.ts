/**
 * Compatibility export for existing Scene UI imports. Rotation coordinates
 * several stores and therefore belongs to the service runtime layer.
 */
export * from '@/services/character-rotation/rotation-runtime-store'
