<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only DUMP protection lets adb drive live lifecycle QA without exposing a production component. -->
        <receiver
            android:name=".Phase12QaReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP" />
    </application>
</manifest>
